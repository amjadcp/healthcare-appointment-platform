# API Documentation

This folder holds exported OpenAPI/Swagger snapshots.

Generate via:
```
# once backend is running
curl http://localhost:8080/v3/api-docs -o docs/api/openapi.json
```

Live Swagger UI (local dev): http://localhost:8080/swagger-ui.html